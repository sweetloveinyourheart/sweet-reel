# Elasticsearch Indices Configuration
# Format: index_name:mapping_filename (without -mapping.json suffix)
# Lines starting with # are comments and will be ignored
# The script will look for mapping files in the elasticsearch/ directory

videos:videos
video_thumbnails:video_thumbnails
users:users
